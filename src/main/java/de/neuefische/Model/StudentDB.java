package de.neuefische.Model;

import java.util.Arrays;

public class StudentDB {
    Student[] students = new Student[Student.getIdCount()];

    public StudentDB(Student[] students) {
        this.students[] = students[];
    }
}